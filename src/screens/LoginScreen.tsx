import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const LoginScreen = (props: Props) => {
  return (
    <View>
      <Text>LogisssnScreen</Text>
    </View>
  );
};

export default LoginScreen;
