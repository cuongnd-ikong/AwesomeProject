import { createAction } from '@reduxjs/toolkit';
const ACTION_PREFIX = 'ACTION-PRODUCT';

const actionGetVoucher = createAction<{}>(`${ACTION_PREFIX}-sagaGetVoucher`);
false && actionGetVoucher;

export const watchProduct = function* () {};

export const actionProduct = {};
